import { GetCompetitionSkillsQuery } from './get-competition-skills.query';

describe('GetCompetitionSkillsQuery', () => {
  it('should be defined', () => {
    expect(new GetCompetitionSkillsQuery()).toBeDefined();
  });
});
