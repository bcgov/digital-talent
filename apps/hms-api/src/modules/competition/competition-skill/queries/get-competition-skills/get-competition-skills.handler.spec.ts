import { GetCompetitionSkillsHandler } from './get-competition-skills.handler';

describe('GetCompetitionSkillsHandler', () => {
  it('should be defined', () => {
    expect(new GetCompetitionSkillsHandler()).toBeDefined();
  });
});
