import { RemoveCompetitionSkillHandler } from './remove-competition-skill.handler';

describe('RemoveCompetitionSkillHandler', () => {
  it('should be defined', () => {
    expect(new RemoveCompetitionSkillHandler()).toBeDefined();
  });
});
