import { AddCompetitionSkillCommand } from './add-competition-skill.command';

describe('AddCompetitionSkillCommand', () => {
  it('should be defined', () => {
    expect(new AddCompetitionSkillCommand()).toBeDefined();
  });
});
