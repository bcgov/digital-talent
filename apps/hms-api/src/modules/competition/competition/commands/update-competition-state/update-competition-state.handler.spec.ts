import { UpdateCompetitionStateHandler } from './update-competition-state.handler';

describe('UpdateCompetitionStateHandler', () => {
  it('should be defined', () => {
    expect(new UpdateCompetitionStateHandler()).toBeDefined();
  });
});
