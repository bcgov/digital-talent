import { GetCompetitionsQuery } from './get-competitions.query';

describe('GetCompetitionsQuery', () => {
  it('should be defined', () => {
    expect(new GetCompetitionsQuery()).toBeDefined();
  });
});
