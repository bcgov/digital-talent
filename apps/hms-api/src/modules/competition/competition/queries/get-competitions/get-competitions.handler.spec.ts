import { GetCompetitionsHandler } from './get-competitions.handler';

describe('GetCompetitionsHandler', () => {
  it('should be defined', () => {
    expect(new GetCompetitionsHandler()).toBeDefined();
  });
});
