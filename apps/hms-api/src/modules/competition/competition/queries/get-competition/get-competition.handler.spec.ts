import { GetCompetitionHandler } from './get-competition.handler';

describe('GetCompetitionHandler', () => {
  it('should be defined', () => {
    expect(new GetCompetitionHandler()).toBeDefined();
  });
});
