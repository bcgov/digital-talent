import { CompetitionDeletedHandler } from './competition-deleted.handler';

describe('CompetitionDeletedHandler', () => {
  it('should be defined', () => {
    expect(new CompetitionDeletedHandler()).toBeDefined();
  });
});
