import { CompetitionUpdatedHandler } from './competition-updated.handler';

describe('CompetitionUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new CompetitionUpdatedHandler()).toBeDefined();
  });
});
