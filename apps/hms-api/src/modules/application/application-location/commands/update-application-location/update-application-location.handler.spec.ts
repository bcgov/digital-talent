import { UpdateApplicationLocationHandler } from './update-application-location.handler';

describe('UpdateApplicationLocationHandler', () => {
  it('should be defined', () => {
    expect(new UpdateApplicationLocationHandler()).toBeDefined();
  });
});
