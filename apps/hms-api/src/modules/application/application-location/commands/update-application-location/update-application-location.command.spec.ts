import { UpdateApplicationLocationCommand } from './update-application-location.command';

describe('UpdateApplicationLocationCommand', () => {
  it('should be defined', () => {
    expect(new UpdateApplicationLocationCommand()).toBeDefined();
  });
});
