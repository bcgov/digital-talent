import { CreateApplicationLocationCommand } from './create-application-location.command';

describe('CreateApplicationLocationCommand', () => {
  it('should be defined', () => {
    expect(new CreateApplicationLocationCommand()).toBeDefined();
  });
});
