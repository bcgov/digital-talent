import { CreateApplicationLocationHandler } from './create-application-location.handler';

describe('CreateApplicationLocationHandler', () => {
  it('should be defined', () => {
    expect(new CreateApplicationLocationHandler()).toBeDefined();
  });
});
