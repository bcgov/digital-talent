import { GetApplicationLocationsQuery } from './get-application-locations.query';

describe('GetApplicationLocationsQuery', () => {
  it('should be defined', () => {
    expect(new GetApplicationLocationsQuery()).toBeDefined();
  });
});
