import { ApplicationSkillUpdatedHandler } from './application-skill-updated.handler';

describe('ApplicationSkillUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new ApplicationSkillUpdatedHandler()).toBeDefined();
  });
});
