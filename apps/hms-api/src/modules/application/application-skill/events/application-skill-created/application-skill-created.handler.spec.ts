import { ApplicationSkillCreatedHandler } from './application-skill-created.handler';

describe('ApplicationSkillCreatedHandler', () => {
  it('should be defined', () => {
    expect(new ApplicationSkillCreatedHandler()).toBeDefined();
  });
});
