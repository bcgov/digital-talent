import { DeleteApplicationSkillHandler } from './delete-application-skill.handler';

describe('DeleteApplicationSkillHandler', () => {
  it('should be defined', () => {
    expect(new DeleteApplicationSkillHandler()).toBeDefined();
  });
});
