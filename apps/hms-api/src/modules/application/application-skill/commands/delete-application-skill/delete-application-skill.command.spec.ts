import { DeleteApplicationSkillCommand } from './delete-application-skill.command';

describe('UpdateApplicationSkillCommand', () => {
  it('should be defined', () => {
    expect(new DeleteApplicationSkillCommand()).toBeDefined();
  });
});
