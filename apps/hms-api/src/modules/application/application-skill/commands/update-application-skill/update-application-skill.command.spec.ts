import { UpdateApplicationSkillCommand } from './update-application-skill.command';

describe('UpdateApplicationSkillCommand', () => {
  it('should be defined', () => {
    expect(new UpdateApplicationSkillCommand()).toBeDefined();
  });
});
