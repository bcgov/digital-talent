import { CreateApplicationSkillCommand } from './create-application-skill.command';

describe('CreateApplicationSkillCommand', () => {
  it('should be defined', () => {
    expect(new CreateApplicationSkillCommand()).toBeDefined();
  });
});
