import { CreateApplicationSkillHandler } from './create-application-skill.handler';

describe('CreateApplicationSkillHandler', () => {
  it('should be defined', () => {
    expect(new CreateApplicationSkillHandler()).toBeDefined();
  });
});
