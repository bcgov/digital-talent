import { GetApplicationSkillsQuery } from './get-application-skills.query';

describe('GetApplicationSkillsQuery', () => {
  it('should be defined', () => {
    expect(new GetApplicationSkillsQuery()).toBeDefined();
  });
});
