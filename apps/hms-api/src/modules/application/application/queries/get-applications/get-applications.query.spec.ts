import { GetApplicationsQuery } from './get-applications.query';

describe('GetApplicationsQuery', () => {
  it('should be defined', () => {
    expect(new GetApplicationsQuery()).toBeDefined();
  });
});
