import { ApplicationUpdatedHandler } from './application-updated.handler';

describe('ApplicationUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new ApplicationUpdatedHandler()).toBeDefined();
  });
});
