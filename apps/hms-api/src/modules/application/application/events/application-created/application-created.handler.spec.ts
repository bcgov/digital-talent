import { ApplicationCreatedHandler } from './application-created.handler';

describe('ApplicationCreatedHandler', () => {
  it('should be defined', () => {
    expect(new ApplicationCreatedHandler()).toBeDefined();
  });
});
