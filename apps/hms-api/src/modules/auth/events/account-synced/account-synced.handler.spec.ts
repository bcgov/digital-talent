import { AccountSyncedHandler } from './account-synced.handler';

describe('AccountSyncedHandler', () => {
  it('should be defined', () => {
    expect(new AccountSyncedHandler()).toBeDefined();
  });
});
