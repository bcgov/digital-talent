import { LocationUpdatedHandler } from './location-updated.handler';

describe('LocationUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new LocationUpdatedHandler()).toBeDefined();
  });
});
