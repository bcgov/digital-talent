import { GetLocationsHandler } from './get-locations.handler';

describe('GetLocationsHandler', () => {
  it('should be defined', () => {
    expect(new GetLocationsHandler()).toBeDefined();
  });
});
