import { UpdateLocationHandler } from './update-location.handler';

describe('UpdateLocationHandler', () => {
  it('should be defined', () => {
    expect(new UpdateLocationHandler()).toBeDefined();
  });
});
