import { DeleteLocationCommand } from './delete-location.command';

describe('UpdateLocationCommand', () => {
  it('should be defined', () => {
    expect(new DeleteLocationCommand()).toBeDefined();
  });
});
