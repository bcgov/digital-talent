import { DeleteLocationHandler } from './delete-location.handler';

describe('DeleteLocationHandler', () => {
  it('should be defined', () => {
    expect(new DeleteLocationHandler()).toBeDefined();
  });
});
