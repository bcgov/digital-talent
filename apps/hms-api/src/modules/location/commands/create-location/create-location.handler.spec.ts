import { CreateLocationHandler } from './create-location.handler';

describe('CreateLocationHandler', () => {
  it('should be defined', () => {
    expect(new CreateLocationHandler()).toBeDefined();
  });
});
