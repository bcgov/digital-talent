import { GetPositionsHandler } from './get-positions.handler';

describe('GetPositionsHandler', () => {
  it('should be defined', () => {
    expect(new GetPositionsHandler()).toBeDefined();
  });
});
