import { UpdateClassificationHandler } from './update-classification.handler';

describe('UpdateClassificationHandler', () => {
  it('should be defined', () => {
    expect(new UpdateClassificationHandler()).toBeDefined();
  });
});
