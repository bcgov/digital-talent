import { UpdateClassificationCommand } from './update-classification.command';

describe('UpdateClassificationCommand', () => {
  it('should be defined', () => {
    expect(new UpdateClassificationCommand()).toBeDefined();
  });
});
