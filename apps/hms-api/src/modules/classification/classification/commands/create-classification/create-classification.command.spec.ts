import { CreateClassificationCommand } from './create-classification.command';

describe('CreateClassificationCommand', () => {
  it('should be defined', () => {
    expect(new CreateClassificationCommand()).toBeDefined();
  });
});
