import { GetClassificationsQuery } from './get-classifications.query';

describe('GetClassificationsQuery', () => {
  it('should be defined', () => {
    expect(new GetClassificationsQuery()).toBeDefined();
  });
});
