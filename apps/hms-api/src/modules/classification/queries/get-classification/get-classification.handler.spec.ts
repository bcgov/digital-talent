import { GetClassificationHandler } from './get-classification.handler';

describe('GetClassificationHandler', () => {
  it('should be defined', () => {
    expect(new GetClassificationHandler()).toBeDefined();
  });
});
