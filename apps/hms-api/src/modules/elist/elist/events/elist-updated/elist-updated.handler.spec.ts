import { ElistUpdatedHandler } from './elist-updated.handler';

describe('ElistUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new ElistUpdatedHandler()).toBeDefined();
  });
});
