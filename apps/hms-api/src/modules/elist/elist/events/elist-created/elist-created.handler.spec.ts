import { ElistCreatedHandler } from './elist-created.handler';

describe('ElistCreatedHandler', () => {
  it('should be defined', () => {
    expect(new ElistCreatedHandler()).toBeDefined();
  });
});
