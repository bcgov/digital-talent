import { ElistOfferUpdatedEvent } from './elist-offer-updated.event';

describe('ElistOfferUpdatedEvent', () => {
  it('should be defined', () => {
    expect(new ElistOfferUpdatedEvent()).toBeDefined();
  });
});
