import { ElistOfferCreatedHandler } from './elist-offer-created.handler';

describe('ElistOfferCreatedHandler', () => {
  it('should be defined', () => {
    expect(new ElistOfferCreatedHandler()).toBeDefined();
  });
});
