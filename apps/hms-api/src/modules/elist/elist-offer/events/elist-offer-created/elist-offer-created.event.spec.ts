import { ElistOfferCreatedEvent } from './elist-offer-created.event';

describe('ElistOfferCreatedEvent', () => {
  it('should be defined', () => {
    expect(new ElistOfferCreatedEvent()).toBeDefined();
  });
});
