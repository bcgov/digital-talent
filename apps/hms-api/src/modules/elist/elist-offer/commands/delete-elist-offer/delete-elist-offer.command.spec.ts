import { DeleteElistOfferCommand } from './delete-elist-offer.command';

describe('UpdateElistOfferCommand', () => {
  it('should be defined', () => {
    expect(new DeleteElistOfferCommand()).toBeDefined();
  });
});
