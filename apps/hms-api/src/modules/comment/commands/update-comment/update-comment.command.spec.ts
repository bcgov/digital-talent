import { UpdateCommentCommand } from './update-comment.command';

describe('UpdateCommentCommand', () => {
  it('should be defined', () => {
    expect(new UpdateCommentCommand()).toBeDefined();
  });
});
