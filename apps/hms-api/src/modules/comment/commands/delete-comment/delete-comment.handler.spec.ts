import { DeleteCommentHandler } from './delete-comment.handler';

describe('DeleteCommentHandler', () => {
  it('should be defined', () => {
    expect(new DeleteCommentHandler()).toBeDefined();
  });
});
