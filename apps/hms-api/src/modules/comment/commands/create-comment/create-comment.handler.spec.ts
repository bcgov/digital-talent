import { CreateCommentHandler } from './create-comment.handler';

describe('CreateCommentHandler', () => {
  it('should be defined', () => {
    expect(new CreateCommentHandler()).toBeDefined();
  });
});
