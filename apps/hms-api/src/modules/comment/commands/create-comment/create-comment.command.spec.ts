import { CreateCommentCommand } from './create-comment.command';

describe('CreateCommentCommand', () => {
  it('should be defined', () => {
    expect(new CreateCommentCommand()).toBeDefined();
  });
});
