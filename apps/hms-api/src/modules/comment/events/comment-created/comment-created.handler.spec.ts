import { CommentCreatedHandler } from './comment-created.handler';

describe('CommentCreatedHandler', () => {
  it('should be defined', () => {
    expect(new CommentCreatedHandler()).toBeDefined();
  });
});
