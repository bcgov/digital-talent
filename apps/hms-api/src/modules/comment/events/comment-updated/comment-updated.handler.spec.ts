import { CommentUpdatedHandler } from './comment-updated.handler';

describe('CommentUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new CommentUpdatedHandler()).toBeDefined();
  });
});
