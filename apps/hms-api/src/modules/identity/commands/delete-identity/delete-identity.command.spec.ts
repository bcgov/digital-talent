import { DeleteIdentityCommand } from './delete-identity.command';

describe('DeleteIdentityCommand', () => {
  it('should be defined', () => {
    expect(new DeleteIdentityCommand()).toBeDefined();
  });
});
