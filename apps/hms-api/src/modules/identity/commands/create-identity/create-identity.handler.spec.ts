import { CreateIdentityHandler } from './create-identity.handler';

describe('CreateIdentityHandler', () => {
  it('should be defined', () => {
    expect(new CreateIdentityHandler()).toBeDefined();
  });
});
