import { IdentityCreatedHandler } from './identity-created.handler';

describe('IdentityCreatedHandler', () => {
  it('should be defined', () => {
    expect(new IdentityCreatedHandler()).toBeDefined();
  });
});
