import { DeleteOpportunitySkillCommand } from './delete-opportunity-skill.command';

describe('UpdateOpportunitySkillCommand', () => {
  it('should be defined', () => {
    expect(new DeleteOpportunitySkillCommand()).toBeDefined();
  });
});
