import { GetOpportunitySkillsQuery } from './get-opportunity-skills.query';

describe('GetOpportunitySkillsQuery', () => {
  it('should be defined', () => {
    expect(new GetOpportunitySkillsQuery()).toBeDefined();
  });
});
