import { OpportunityCreatedHandler } from './opportunity-created.handler';

describe('OpportunityCreatedHandler', () => {
  it('should be defined', () => {
    expect(new OpportunityCreatedHandler()).toBeDefined();
  });
});
