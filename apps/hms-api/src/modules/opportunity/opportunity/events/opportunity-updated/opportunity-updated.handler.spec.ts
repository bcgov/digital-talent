import { OpportunityUpdatedHandler } from './opportunity-updated.handler';

describe('OpportunityUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new OpportunityUpdatedHandler()).toBeDefined();
  });
});
