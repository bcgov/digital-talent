import { GetOpportunityHandler } from './get-opportunity.handler';

describe('GetOpportunityHandler', () => {
  it('should be defined', () => {
    expect(new GetOpportunityHandler()).toBeDefined();
  });
});
