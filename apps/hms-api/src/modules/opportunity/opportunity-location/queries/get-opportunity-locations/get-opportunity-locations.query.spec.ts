import { GetOpportunityLocationsQuery } from './get-opportunity-locations.query';

describe('GetOpportunityLocationsQuery', () => {
  it('should be defined', () => {
    expect(new GetOpportunityLocationsQuery()).toBeDefined();
  });
});
