import { GetMinistriesHandler } from './get-ministries.handler';

describe('GetMinistriesHandler', () => {
  it('should be defined', () => {
    expect(new GetMinistriesHandler()).toBeDefined();
  });
});
