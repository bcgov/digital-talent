import { MinistryUpdatedHandler } from './ministry-updated.handler';

describe('MinistryUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new MinistryUpdatedHandler()).toBeDefined();
  });
});
