import { CreateMinistryHandler } from './create-ministry.handler';

describe('CreateMinistryHandler', () => {
  it('should be defined', () => {
    expect(new CreateMinistryHandler()).toBeDefined();
  });
});
