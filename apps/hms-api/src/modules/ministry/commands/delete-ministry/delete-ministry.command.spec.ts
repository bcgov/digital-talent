import { DeleteMinistryCommand } from './delete-ministry.command';

describe('DeleteMinistryCommand', () => {
  it('should be defined', () => {
    expect(new DeleteMinistryCommand()).toBeDefined();
  });
});
