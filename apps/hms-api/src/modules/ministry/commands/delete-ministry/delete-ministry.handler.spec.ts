import { DeleteMinistryHandler } from './delete-ministry.handler';

describe('DeleteMinistryHandler', () => {
  it('should be defined', () => {
    expect(new DeleteMinistryHandler()).toBeDefined();
  });
});
