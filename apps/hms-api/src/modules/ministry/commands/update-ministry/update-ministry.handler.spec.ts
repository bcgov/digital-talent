import { UpdateMinistryHandler } from './update-ministry.handler';

describe('UpdateMinistryHandler', () => {
  it('should be defined', () => {
    expect(new UpdateMinistryHandler()).toBeDefined();
  });
});
