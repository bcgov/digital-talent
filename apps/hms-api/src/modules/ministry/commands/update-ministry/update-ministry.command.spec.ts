import { UpdateMinistryCommand } from './update-ministry.command';

describe('UpdateMinistryCommand', () => {
  it('should be defined', () => {
    expect(new UpdateMinistryCommand()).toBeDefined();
  });
});
