import { SkillDeletedHandler } from './skill-deleted.handler';

describe('SkillDeletedHandler', () => {
  it('should be defined', () => {
    expect(new SkillDeletedHandler()).toBeDefined();
  });
});
