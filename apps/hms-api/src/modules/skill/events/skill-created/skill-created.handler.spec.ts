import { SkillCreatedHandler } from './skill-created.handler';

describe('SkillCreatedHandler', () => {
  it('should be defined', () => {
    expect(new SkillCreatedHandler()).toBeDefined();
  });
});
