import { CreateCompetitionScheduleHandler } from './create-competition-schedule.handler';

describe('CreateCompetitionScheduleHandler', () => {
  it('should be defined', () => {
    expect(new CreateCompetitionScheduleHandler()).toBeDefined();
  });
});
