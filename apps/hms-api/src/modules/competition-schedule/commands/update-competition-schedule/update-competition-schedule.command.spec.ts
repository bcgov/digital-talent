import { UpdateCompetitionScheduleCommand } from './update-competition-schedule.command';

describe('UpdateCompetitionScheduleCommand', () => {
  it('should be defined', () => {
    expect(new UpdateCompetitionScheduleCommand()).toBeDefined();
  });
});
