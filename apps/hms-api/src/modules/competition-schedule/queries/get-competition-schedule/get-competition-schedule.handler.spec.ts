import { GetCompetitionScheduleHandler } from './get-competition-schedule.handler';

describe('GetCompetitionScheduleHandler', () => {
  it('should be defined', () => {
    expect(new GetCompetitionScheduleHandler()).toBeDefined();
  });
});
