import { GetCompetitionScheduleQuery } from './get-competition-schedule.query';

describe('GetCompetitionScheduleQuery', () => {
  it('should be defined', () => {
    expect(new GetCompetitionScheduleQuery()).toBeDefined();
  });
});
