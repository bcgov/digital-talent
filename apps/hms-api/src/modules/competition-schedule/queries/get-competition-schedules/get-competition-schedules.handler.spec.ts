import { GetCompetitionSchedulesHandler } from './get-competition-schedules.handler';

describe('GetCompetitionSchedulesHandler', () => {
  it('should be defined', () => {
    expect(new GetCompetitionSchedulesHandler()).toBeDefined();
  });
});
