import { OccupationGroupCreatedHandler } from './occupation-group-created.handler';

describe('OccupationGroupCreatedHandler', () => {
  it('should be defined', () => {
    expect(new OccupationGroupCreatedHandler()).toBeDefined();
  });
});
