import { OccupationGroupUpdatedEvent } from './occupation-group-updated.event';

describe('OccupationGroupUpdatedEvent', () => {
  it('should be defined', () => {
    expect(new OccupationGroupUpdatedEvent()).toBeDefined();
  });
});
