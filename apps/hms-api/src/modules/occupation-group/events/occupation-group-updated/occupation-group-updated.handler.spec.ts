import { OccupationGroupUpdatedHandler } from './occupation-group-updated.handler';

describe('OccupationGroupUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new OccupationGroupUpdatedHandler()).toBeDefined();
  });
});
