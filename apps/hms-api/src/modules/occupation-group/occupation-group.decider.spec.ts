import { OccupationGroupDecider } from './occupation-group.decider';

describe('OccupationGroupDecider', () => {
  it('should be defined', () => {
    expect(new OccupationGroupDecider()).toBeDefined();
  });
});
