import { JobDescriptionDecider } from './job-description.decider';

describe('JobDescriptionDecider', () => {
  it('should be defined', () => {
    expect(new JobDescriptionDecider()).toBeDefined();
  });
});
