import { JobDescriptionCreatedHandler } from './job-description-created.handler';

describe('JobDescriptionCreatedHandler', () => {
  it('should be defined', () => {
    expect(new JobDescriptionCreatedHandler()).toBeDefined();
  });
});
