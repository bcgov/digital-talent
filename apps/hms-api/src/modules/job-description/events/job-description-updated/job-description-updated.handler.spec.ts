import { JobDescriptionUpdatedHandler } from './job-description-updated.handler';

describe('JobDescriptionUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new JobDescriptionUpdatedHandler()).toBeDefined();
  });
});
