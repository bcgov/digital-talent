import { DeleteJobDescriptionHandler } from './delete-job-description.handler';

describe('DeleteJobDescriptionHandler', () => {
  it('should be defined', () => {
    expect(new DeleteJobDescriptionHandler()).toBeDefined();
  });
});
