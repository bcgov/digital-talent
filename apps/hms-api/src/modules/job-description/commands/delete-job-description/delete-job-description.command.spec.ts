import { DeleteJobDescriptionCommand } from './delete-job-description.command';

describe('DeleteJobDescriptionCommand', () => {
  it('should be defined', () => {
    expect(new DeleteJobDescriptionCommand()).toBeDefined();
  });
});
