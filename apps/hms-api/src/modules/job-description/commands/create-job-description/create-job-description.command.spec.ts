import { CreateJobDescriptionCommand } from './create-job-description.command';

describe('CreateJobDescriptionCommand', () => {
  it('should be defined', () => {
    expect(new CreateJobDescriptionCommand()).toBeDefined();
  });
});
