import { CreateJobDescriptionHandler } from './create-job-description.handler';

describe('CreateJobDescriptionHandler', () => {
  it('should be defined', () => {
    expect(new CreateJobDescriptionHandler()).toBeDefined();
  });
});
