import { UpdateJobDescriptionCommand } from './update-job-description.command';

describe('UpdateJobDescriptionCommand', () => {
  it('should be defined', () => {
    expect(new UpdateJobDescriptionCommand()).toBeDefined();
  });
});
