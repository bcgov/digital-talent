import { SyncMinistryHandler } from './sync-ministry.handler';

describe('SyncMinistryHandler', () => {
  it('should be defined', () => {
    expect(new SyncMinistryHandler()).toBeDefined();
  });
});
