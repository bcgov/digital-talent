import { CreateMinistryCommand } from './create-ministry.command';

describe('CreateMinistryCommand', () => {
  it('should be defined', () => {
    expect(new CreateMinistryCommand()).toBeDefined();
  });
});
