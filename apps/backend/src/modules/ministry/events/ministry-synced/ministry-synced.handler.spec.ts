import { MinistrySyncedHandler } from './ministry-synced.handler';

describe('MinistrySyncedHandler', () => {
  it('should be defined', () => {
    expect(new MinistrySyncedHandler()).toBeDefined();
  });
});
