import { LocationCreatedHandler } from './location-created.handler';

describe('LocationCreatedHandler', () => {
  it('should be defined', () => {
    expect(new LocationCreatedHandler()).toBeDefined();
  });
});
