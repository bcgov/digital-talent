import { DeleteCompetitionScheduleHandler } from './delete-competition-schedule.handler';

describe('DeleteCompetitionScheduleHandler', () => {
  it('should be defined', () => {
    expect(new DeleteCompetitionScheduleHandler()).toBeDefined();
  });
});
