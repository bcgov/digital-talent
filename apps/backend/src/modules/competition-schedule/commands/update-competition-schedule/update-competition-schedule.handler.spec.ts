import { UpdateCompetitionScheduleHandler } from './update-competition-schedule.handler';

describe('UpdateCompetitionScheduleHandler', () => {
  it('should be defined', () => {
    expect(new UpdateCompetitionScheduleHandler()).toBeDefined();
  });
});
