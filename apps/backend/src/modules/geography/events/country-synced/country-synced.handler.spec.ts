import { CountrySyncedHandler } from './country-synced.handler';

describe('CountrySyncedHandler', () => {
  it('should be defined', () => {
    expect(new CountrySyncedHandler()).toBeDefined();
  });
});
