import { ProvinceSyncedEvent } from './province-synced.event';

describe('ProvinceSyncedEvent', () => {
  it('should be defined', () => {
    expect(new ProvinceSyncedEvent()).toBeDefined();
  });
});
