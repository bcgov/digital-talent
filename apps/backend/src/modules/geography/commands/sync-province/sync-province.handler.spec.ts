import { SyncProvinceHandler } from './sync-province.handler';

describe('SyncProvinceHandler', () => {
  it('should be defined', () => {
    expect(new SyncProvinceHandler()).toBeDefined();
  });
});
