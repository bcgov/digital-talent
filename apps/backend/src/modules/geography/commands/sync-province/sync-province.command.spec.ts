import { SyncProvinceCommand } from './sync-province.command';

describe('SyncProvinceCommand', () => {
  it('should be defined', () => {
    expect(new SyncProvinceCommand()).toBeDefined();
  });
});
