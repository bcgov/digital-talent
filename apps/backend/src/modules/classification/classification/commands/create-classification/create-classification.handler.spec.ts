import { CreateClassificationHandler } from './create-classification.handler';

describe('CreateClassificationHandler', () => {
  it('should be defined', () => {
    expect(new CreateClassificationHandler()).toBeDefined();
  });
});
