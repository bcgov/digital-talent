import { ClassificationCreatedHandler } from './classification-created.handler';

describe('ClassificationCreatedHandler', () => {
  it('should be defined', () => {
    expect(new ClassificationCreatedHandler()).toBeDefined();
  });
});
