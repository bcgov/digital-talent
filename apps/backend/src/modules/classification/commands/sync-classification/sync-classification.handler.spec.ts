import { SyncClassificationHandler } from './sync-classification.handler';

describe('SyncClassificationHandler', () => {
  it('should be defined', () => {
    expect(new SyncClassificationHandler()).toBeDefined();
  });
});
