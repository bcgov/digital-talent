import { SyncClassificationCommand } from './sync-classification.command';

describe('SyncClassificationCommand', () => {
  it('should be defined', () => {
    expect(new SyncClassificationCommand()).toBeDefined();
  });
});
