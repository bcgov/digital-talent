import { DeletePositionHandler } from './delete-position.handler';

describe('DeletePositionHandler', () => {
  it('should be defined', () => {
    expect(new DeletePositionHandler()).toBeDefined();
  });
});
