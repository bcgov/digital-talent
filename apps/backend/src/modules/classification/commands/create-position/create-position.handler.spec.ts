import { CreatePositionHandler } from './create-position.handler';

describe('CreatePositionHandler', () => {
  it('should be defined', () => {
    expect(new CreatePositionHandler()).toBeDefined();
  });
});
