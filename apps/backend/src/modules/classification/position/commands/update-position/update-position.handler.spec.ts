import { UpdatePositionHandler } from './update-position.handler';

describe('UpdatePositionHandler', () => {
  it('should be defined', () => {
    expect(new UpdatePositionHandler()).toBeDefined();
  });
});
