import { ClassificationSyncedEvent } from './classification-synced.event';

describe('ClassificationSyncedEvent', () => {
  it('should be defined', () => {
    expect(new ClassificationSyncedEvent()).toBeDefined();
  });
});
