import { PositionUpdatedHandler } from './position-updated.handler';

describe('PositionUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new PositionUpdatedHandler()).toBeDefined();
  });
});
