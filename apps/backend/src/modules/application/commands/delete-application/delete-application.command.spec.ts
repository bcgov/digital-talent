import { DeleteApplicationCommand } from './delete-application.command';

describe('UpdateApplicationCommand', () => {
  it('should be defined', () => {
    expect(new DeleteApplicationCommand()).toBeDefined();
  });
});
