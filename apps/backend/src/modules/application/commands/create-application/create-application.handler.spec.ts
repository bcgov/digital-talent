import { CreateApplicationHandler } from './create-application.handler';

describe('CreateApplicationHandler', () => {
  it('should be defined', () => {
    expect(new CreateApplicationHandler()).toBeDefined();
  });
});
