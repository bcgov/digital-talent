import { AddCompetitionSkillHandler } from './add-competition-skill.handler';

describe('AddCompetitionSkillHandler', () => {
  it('should be defined', () => {
    expect(new AddCompetitionSkillHandler()).toBeDefined();
  });
});
