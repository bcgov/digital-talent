import { RemoveCompetitionSkillCommand } from './remove-competition-skill.command';

describe('RemoveCompetitionSkillCommand', () => {
  it('should be defined', () => {
    expect(new RemoveCompetitionSkillCommand()).toBeDefined();
  });
});
