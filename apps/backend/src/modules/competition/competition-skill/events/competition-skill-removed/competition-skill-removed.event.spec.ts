import { CompetitionSkillRemovedEvent } from './competition-skill-removed.event';

describe('CompetitionSkillRemovedEvent', () => {
  it('should be defined', () => {
    expect(new CompetitionSkillRemovedEvent()).toBeDefined();
  });
});
