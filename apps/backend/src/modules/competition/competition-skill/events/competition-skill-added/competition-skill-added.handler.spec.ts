import { CompetitionSkillAddedHandler } from './competition-skill-added.handler';

describe('CompetitionSkillAddedHandler', () => {
  it('should be defined', () => {
    expect(new CompetitionSkillAddedHandler()).toBeDefined();
  });
});
