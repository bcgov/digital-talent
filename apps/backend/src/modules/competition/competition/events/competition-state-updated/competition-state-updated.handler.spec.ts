import { CompetitionStateUpdatedHandler } from './competition-state-updated.handler';

describe('CompetitionStateUpdatedHandler', () => {
  it('should be defined', () => {
    expect(new CompetitionStateUpdatedHandler()).toBeDefined();
  });
});
