import { UpdateCompetitionHandler } from './update-competition.handler';

describe('UpdateCompetitionHandler', () => {
  it('should be defined', () => {
    expect(new UpdateCompetitionHandler()).toBeDefined();
  });
});
