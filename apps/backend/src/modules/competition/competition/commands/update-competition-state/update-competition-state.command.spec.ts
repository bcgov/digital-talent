import { UpdateCompetitionStateCommand } from './update-competition-state.command';

describe('UpdateCompetitionStateCommand', () => {
  it('should be defined', () => {
    expect(new UpdateCompetitionStateCommand()).toBeDefined();
  });
});
