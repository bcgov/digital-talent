import { DeleteCompetitionHandler } from './delete-competition.handler';

describe('DeleteCompetitionHandler', () => {
  it('should be defined', () => {
    expect(new DeleteCompetitionHandler()).toBeDefined();
  });
});
