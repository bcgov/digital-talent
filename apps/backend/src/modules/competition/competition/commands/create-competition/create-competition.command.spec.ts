import { CreateCompetitionCommand } from './create-competition.command';

describe('CreateCompetitionCommand', () => {
  it('should be defined', () => {
    expect(new CreateCompetitionCommand()).toBeDefined();
  });
});
